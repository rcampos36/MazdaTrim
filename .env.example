# MazdaTrim runs without secrets by default (trim data and images are in code / remote URLs).
# Copy to .env.local if you add API keys or feature flags later.
#
# NEXT_PUBLIC_APP_URL=http://localhost:3000
